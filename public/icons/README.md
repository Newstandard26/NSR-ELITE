# App Icons

Add the following PNG icons here for full PWA installability:

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512 (used as maskable too)

Use the NSR logo on a black (#000000) background. Until these are added the
manifest will reference missing files but the app still runs.
